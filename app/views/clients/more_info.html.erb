<h2>Заполнение профиля</h2>

<%= form_for @client, :html => {:multipart => true} do |f| %>
	<p>
		<%= f.label :city %>
		<%= f.text_field :city %>
	</p>
	<p>
		<%= f.label :birthday %>
		<%= f.text_field :birthday %>
	</p>
	<p>
		<%= f.file_field :photo %>
	</p>
	<p><%= f.submit %></p>

<% end %>